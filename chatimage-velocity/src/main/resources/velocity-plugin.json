{
  "id": "chatimage",
  "name": "${plugin.name}",
  "authors": ["${plugin.author}"],
  "url": "${plugin.website}",
  "description": "${plugin.description}",
  "version": "${project.version}",
  "main": "io.github.chickensaysbak.chatimage.velocity.PluginVelocity",
  "dependencies": []
}